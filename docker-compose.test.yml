version: '3.8'

services:
  test-db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: degreebaba_ai_test
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
    ports:
      - "5433:5432"
